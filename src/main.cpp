
#include "../include/app.hpp"
#include "../include/player.hpp"

App *app = App::ptr();
int main() {

  //

  return 0;
}

/*
app->init();

while (app->running) {
  app->inputs();
  app->update();
  app->render();
}

app->clean();
 */